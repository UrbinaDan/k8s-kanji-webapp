# k8s-kanji-webapp
Mini kanji spaced repetition trainer deployed with Docker + Kubernetes.

## Features
- Reveal reading + meaning
- Self-grade: Again / Hard / Good / Easy
- Score and streak
- Session goal progress bar
- Weighted reappearance (simple spaced repetition)

## Run locally
```bash
npm install
npm start
# http://localhost:3000
